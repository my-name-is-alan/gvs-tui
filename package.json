{
  "name": "gvs-tui",
  "private": true,
  "version": "0.2.3",
  "type": "module",
  "engines": {
    "bun": ">=1.3.0",
    "node": ">=26.3.0"
  },
  "scripts": {
    "dev": "bun --bun vite --force",
    "dev:clean": "bun run scripts/dev-clean.ts && bun --bun vite --force",
    "build": "tsc --noEmit && vite build",
    "setup": "bun run tools:prepare",
    "tools:prepare": "bun run scripts/prepare-tools.ts",
    "tools:check": "bun run scripts/check-repo-tools.ts",
    "start": "bun run build && bun --bun dist/main.js",
    "preview": "bun run scripts/preview.ts",
    "demo": "bun run build && bun run scripts/demo.ts"
  },
  "dependencies": {
    "@opentui/core": "^0.5.11",
    "qrcode": "^1.5.4",
    "vue": "^3.5.40",
    "vue-termui": "^0.3.0"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.5",
    "@vitejs/plugin-vue": "^6.0.0",
    "@types/bun": "^1.3.5",
    "@types/node": "^24.13.3",
    "typescript": "^6.0.2",
    "vite": "^8.0.0"
  }
}
